#!/usr/bin/env sh
set -e

if ! command -v pnpm >/dev/null 2>&1; then
  echo "pnpm is required to validate commit messages." >&2
  exit 1
fi

case "$(cat "$1")" in
  Merge*|Revert*|fixup\!*|squash\!*)
    exit 0
    ;;
esac

pnpm exec commitlint --config commitlint.config.mjs --edit "$1"
